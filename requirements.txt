# requirements.txt
# Run: pip install -r requirements.txt

fastapi         # The web framework
uvicorn[standard]  # ASGI server to run FastAPI

pymongo           # Async MongoDB driver
certifi           # CA bundle for MongoDB SSL connections
pymongo[srv]
aioboto3          # Async wrapper around boto3 for S3 uploads and pre-signed URLs

aiosmtplib       # Async SMTP client for sending Gmail
pydantic           # Data validation (used by FastAPI)
pydantic-settings  # Loads settings from .env file
python-dotenv      # Reads .env files
python-multipart   # Required for FastAPI to handle file uploads (Form + File)
gunicorn
pydantic[email]
